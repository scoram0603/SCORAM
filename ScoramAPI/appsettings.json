{
  "ConnectionStrings": {
    "DefaultConnection": ""
  },
  "Jwt": {
    "Key": "",
    "Issuer": "ScoramAPI",
    "Audience": "ScoramClient",
    "ExpiryMinutes": "1440"
  },
  "Meilisearch": {
    "Url": "http://localhost:7700",
    "ApiKey": ""
  },
  "AzureBlobStorage": {
    "_COMMENT": "DEMO ONLY -- REPLACE ConnectionString WITH YOUR REAL AZURE STORAGE CONNECTION STRING. Azure Portal -> scoramstorage -> Security + networking -> Access keys -> Key 1 -> Connection string. Never commit the real value -- see appsettings.Production.json (gitignored) or environment variables instead.",
    "ConnectionString": "",
    "ContainerName": "uploads",
    "MaxFileSizeMB": 100
  },
  "Msg91": {
    "_COMMENT": "DEMO ONLY -- REPLACE AuthKey WITH YOUR REAL MSG91 AUTH KEY (MSG91 dashboard -> Authkey, NOT the OTP widget's own tokenAuth -- see Msg91Service's own comment on the difference). Never commit the real value -- see appsettings.Production.json (gitignored) or environment variables instead. WidgetId/TokenAuth are the OTP WIDGET's own client-facing credentials (MSG91 dashboard -> OTP -> Widget) -- these identify which widget config to load, not a server secret, so it's fine for GET /api/auth/otp-widget-config to hand them to the mobile app (same as ScoramWeb's own JS bundle already embeds them for the browser widget).",
    "AuthKey": "",
    "WidgetId": "",
    "TokenAuth": ""
  },
  "Firebase": {
    "_COMMENT": "DEMO ONLY -- mobile push (FCM). ServiceAccountKeyPath is a path on THIS SERVER to the service-account JSON you download from Firebase Console -> Project settings -> Service accounts -> Generate new private key. Leave blank to leave mobile push disabled (see Services/PushNotificationService.cs -- same no-op-until-configured pattern as VapidKeys/Msg91). Never commit the real JSON file -- keep it outside the repo (or gitignored) same as the Azure connection string above.",
    "ServiceAccountKeyPath": ""
  },
  "Captcha": {
    "_COMMENT": "Signing secret for the app's own lightweight math captcha (see Services/CaptchaService.cs) -- NOT a third-party key, generate any long random string yourself (e.g. `openssl rand -base64 32`) and keep it out of source control same as Jwt:Key. Falls back to Jwt:Key if left blank so a fresh clone still works out of the box, but a dedicated secret here is safer for production so a captcha-token leak/replay can't be combined with anything JWT-related.",
    "SigningKey": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    },
    "Enrich": [ "FromLogContext" ],
    "WriteTo": [
      { "Name": "Console" },
      {
        "Name": "File",
        "Args": {
          "path": "logs/scoram-.log",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 14
        }
      }
    ]
  },
  "AllowedHosts": "*"
}
